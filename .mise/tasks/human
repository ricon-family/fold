#!/usr/bin/env bash
#MISE description="Return the path to the HUMAN.md file"
set -eo pipefail

if [ -z "${HUMAN_MD:-}" ]; then
  echo "Error: HUMAN_MD environment variable is not set" >&2
  echo "Set it in your .zshrc, e.g.: export HUMAN_MD=~/path/to/HUMAN.md" >&2
  exit 1
fi

echo "$HUMAN_MD"
