# ⚠️  AUTO-GENERATED — do not edit this file directly.
# Source: shimmer workflows generate
# Regenerate: shimmer workflows generate

name: 'zeke'

on:
  workflow_dispatch:
    inputs:
      message:
        description: 'Message for the agent'
        required: true
        type: string
      model:
        description: 'Provider-qualified model to use (e.g., openai-codex/gpt-5.5)'
        required: true
        type: string
  workflow_call:
    inputs:
      message:
        description: 'Message for the agent'
        required: true
        type: string
      model:
        description: 'Provider-qualified model to use (e.g., openai-codex/gpt-5.5)'
        required: true
        type: string
    secrets:
      ANTHROPIC_API_KEY:
        required: false
      HF_TOKEN:
        required: false
      PI_AUTH_JSON:
        required: false
      ZEKE_GITHUB_PAT:
        required: true
      ZEKE_GPG_PRIVATE_KEY:
        required: true
      ZEKE_EMAIL_PASSWORD:
        required: true
      ZEKE_GITHUB_PASSWORD:
        required: false
      ZEKE_B2_ENDPOINT:
        required: false
      ZEKE_B2_KEY_ID:
        required: false
      ZEKE_B2_APPLICATION_KEY:
        required: false
      ZEKE_B2_BUCKET:
        required: false

jobs:
  run:
    uses: ./.github/workflows/agent-run.yml
    with:
      agent: 'zeke'
      message: ${{ inputs.message }}
      model: ${{ inputs.model }}
    secrets:
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
      HF_TOKEN: ${{ secrets.HF_TOKEN }}
      PI_AUTH_JSON: ${{ secrets.PI_AUTH_JSON }}
      AGENT_GITHUB_PAT: ${{ secrets.ZEKE_GITHUB_PAT }}
      AGENT_GPG_PRIVATE_KEY: ${{ secrets.ZEKE_GPG_PRIVATE_KEY }}
      AGENT_EMAIL_PASSWORD: ${{ secrets.ZEKE_EMAIL_PASSWORD }}
      AGENT_GITHUB_PASSWORD: ${{ secrets.ZEKE_GITHUB_PASSWORD }}
      AGENT_B2_ENDPOINT: ${{ secrets.ZEKE_B2_ENDPOINT }}
      AGENT_B2_KEY_ID: ${{ secrets.ZEKE_B2_KEY_ID }}
      AGENT_B2_APPLICATION_KEY: ${{ secrets.ZEKE_B2_APPLICATION_KEY }}
      AGENT_B2_BUCKET: ${{ secrets.ZEKE_B2_BUCKET }}
