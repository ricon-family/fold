# ⚠️  AUTO-GENERATED — do not edit this file directly.
# Source: shimmer/.github/templates/agent-scheduled.yml + workflows.yaml
# Regenerate: shimmer workflows generate

name: "rho-repo-audit"

on:
  schedule:
    - cron: '0 16 * * 1'
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/rho.yml
    with:
      message: "Read .jobs/repo-audit.txt in fold and execute those instructions."
      model: "openai-codex/gpt-5.5"
    secrets: inherit
