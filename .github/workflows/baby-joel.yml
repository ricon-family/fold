# ⚠️  AUTO-GENERATED — do not edit this file directly.
# Source: shimmer workflows generate
# Regenerate: shimmer workflows generate

name: 'baby-joel'

on:
  workflow_dispatch:
    inputs:
      message:
        description: 'Message for the agent'
        required: true
        type: string
      model:
        description: 'Provider-qualified model to use (e.g., openai-codex/gpt-5.5)'
        required: true
        type: string
  workflow_call:
    inputs:
      message:
        description: 'Message for the agent'
        required: true
        type: string
      model:
        description: 'Provider-qualified model to use (e.g., openai-codex/gpt-5.5)'
        required: true
        type: string
    secrets:
      ANTHROPIC_API_KEY:
        required: false
      HF_TOKEN:
        required: false
      PI_AUTH_JSON:
        required: false
      BABY_JOEL_GITHUB_PAT:
        required: true
      BABY_JOEL_GPG_PRIVATE_KEY:
        required: true
      BABY_JOEL_EMAIL_PASSWORD:
        required: true
      BABY_JOEL_GITHUB_PASSWORD:
        required: false
      BABY_JOEL_B2_ENDPOINT:
        required: false
      BABY_JOEL_B2_KEY_ID:
        required: false
      BABY_JOEL_B2_APPLICATION_KEY:
        required: false
      BABY_JOEL_B2_BUCKET:
        required: false

jobs:
  run:
    uses: ./.github/workflows/agent-run.yml
    with:
      agent: 'baby-joel'
      message: ${{ inputs.message }}
      model: ${{ inputs.model }}
    secrets:
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
      HF_TOKEN: ${{ secrets.HF_TOKEN }}
      PI_AUTH_JSON: ${{ secrets.PI_AUTH_JSON }}
      AGENT_GITHUB_PAT: ${{ secrets.BABY_JOEL_GITHUB_PAT }}
      AGENT_GPG_PRIVATE_KEY: ${{ secrets.BABY_JOEL_GPG_PRIVATE_KEY }}
      AGENT_EMAIL_PASSWORD: ${{ secrets.BABY_JOEL_EMAIL_PASSWORD }}
      AGENT_GITHUB_PASSWORD: ${{ secrets.BABY_JOEL_GITHUB_PASSWORD }}
      AGENT_B2_ENDPOINT: ${{ secrets.BABY_JOEL_B2_ENDPOINT }}
      AGENT_B2_KEY_ID: ${{ secrets.BABY_JOEL_B2_KEY_ID }}
      AGENT_B2_APPLICATION_KEY: ${{ secrets.BABY_JOEL_B2_APPLICATION_KEY }}
      AGENT_B2_BUCKET: ${{ secrets.BABY_JOEL_B2_BUCKET }}
