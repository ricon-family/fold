# Generated by `shimmer workflows:generate`.
# Do not edit directly — regenerate with: shimmer workflows:generate

name: quick

on:
  workflow_dispatch:
    inputs:
      message:
        description: 'Message for the agent'
        required: true
        type: string
      model:
        description: 'Claude model to use (optional)'
        required: false
        type: string

jobs:
  run:
    uses: ./.github/workflows/agent-run.yml
    with:
      agent: quick
      message: ${{ inputs.message }}
      model: ${{ inputs.model }}
    secrets:
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
      AGENT_GITHUB_PAT: ${{ secrets.QUICK_GITHUB_PAT }}
      AGENT_GPG_PRIVATE_KEY: ${{ secrets.QUICK_GPG_PRIVATE_KEY }}
      AGENT_EMAIL_PASSWORD: ${{ secrets.QUICK_EMAIL_PASSWORD }}
      AGENT_MATRIX_PASSWORD: ${{ secrets.QUICK_MATRIX_PASSWORD }}
      AGENT_GITHUB_PASSWORD: ${{ secrets.QUICK_GITHUB_PASSWORD }}
