# ⚠️  AUTO-GENERATED — do not edit this file directly.
# Source: shimmer/.github/templates/agent-scheduled.yml + workflows.yaml
# Regenerate: shimmer workflows generate

name: "junior-daily-checkin"

on:
  schedule:
    - cron: '0 15 * * *'
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/junior.yml
    with:
      message: "Check your home repo for job instructions at jobs/dc-checkin.txt and execute them."
      model: "openai-codex/gpt-5.5"
    secrets: inherit
