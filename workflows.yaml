# yaml-language-server: $schema=https://raw.githubusercontent.com/ricon-family/shimmer/main/workflows.schema.json
#
# Agent workflow schedules for fold.
# Run `shimmer workflows:generate` to regenerate workflow files.
#
# Agent identities are in fold/agents/.
# Job definitions live in each work repo's .jobs/ directory.

workflows:
  - name: ghl-referral
    agent: c0da
    model: claude-sonnet-4-5-20250929
    schedule: "*/30 14-22 * * 1-5"
    message: "Clone ricon-family/grow-heal-love to your workspace and read .jobs/referral-check.txt for instructions."

# Other workflows (uncomment when ready):
#
#   - name: quick-probe
#     agent: quick
#     schedule: "0 */4 * * *"
#     message: |
#       Clone ricon-family/shimmer to your workspace.
#       Read .jobs/probe.txt and execute those instructions.
